@model Product

@*<div class="col-4">
    <div class="card mb-2">
        <div class="card-img text-center p-2" >
            <a asp-controller="FoodSite" asp-action="Details" asp-route-url="@Model.Url">
                <img src="~/images/@Model.ImageUrl" class="img-thumbnail" alt="" width="50"/>
            </a>
        </div>
        <div class="card-body">
            <h5 class="card-title text-primary text-center">@Model.Name</h5>
            <div class="text-muted ps-2 text-center">@Model.Properties</div>
        </div>
    </div>
    <div>
        <a class="btn btn-sm btn-primary" asp-controller="FoodSite" asp-action="Details" asp-route-url="@Model.Url">Details</a>
    </div>
</div>*@

<div class="d-flex  row-cols-1 row-cols-md-2 g-4">
    <div class="col">
        <div class="card">
            <img src="~/images/@Model.ImageUrl" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">@Model.Name</h5>
                <p class="card-text">@Model.Properties</p>
            </div>
        </div>
        <div class="card-footer">
            <small class="text-muted"><a class="btn btn-sm btn-primary" asp-controller="FoodSite" asp-action="Details" asp-route-url="@Model.Url">Details</a></small>
        </div>
    </div>
</div>