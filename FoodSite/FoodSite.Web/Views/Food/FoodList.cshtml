@model List<Food>

<div class="container">
    @if (Model.Count > 0)
    {
        <div>
            @await Html.PartialAsync("_FoodList")
        </div>
    }
    else
    {
        <div>No Foods </div>
    }
</div>